asgiref==3.8.1
Django==5.0.6
gunicorn==22.0.0
psycopg2-binary==2.9.9           # binary, no build toolchain needed
sqlparse==0.5.0
whitenoise==6.7.0
django-cors-headers==4.4.0
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

# video pipeline (CPU-only, small)
numpy==2.0.2
opencv-python-headless==4.10.0.84

# OPTIONAL: YOLOv8 (pulls PyTorch CPU; large & may fail on Railway)
# ultralytics==8.2.103
# torch==2.3.1
# torchvision==0.18.1
